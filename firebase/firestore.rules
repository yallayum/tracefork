rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Demo / capstone: read-only for clients; writes via Admin SDK (seed script).
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
